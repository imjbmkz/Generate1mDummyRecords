drop table fake_person;
create table fake_person (
	id bigint,
	full_name varchar(256),
	is_male boolean,
	dob date,
	ssn varchar(50),
	email varchar(256),
	phone varchar(50),
	address varchar(256),
	city varchar(50),
	state varchar(50),
	zipcode varchar(50),
	country varchar(50)
)

